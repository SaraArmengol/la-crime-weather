# Notebooks

Notebooks tell the story; `src/` does the work. A notebook should **import** functions and read files from `data/processed/`, never define cleaning logic.

- `01_exploration.ipynb`: scratch exploration (optional to publish)
- `02_findings.ipynb`: the narrative behind the README's key findings; saves final charts to `reports/figures/`
